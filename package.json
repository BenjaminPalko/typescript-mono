{
  "name": "typescript-mono",
  "description": "",
  "private": true,
  "author": "Benjamin Palko",
  "license": "ISC",
  "version": "1.0.0",
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "clean": "lerna run clean",
    "build": "lerna run build",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "prepare": "npm run bootstrap && npm run clean && npm run lint && npm run test && npm run build"
  },
  "dependencies": {
  },
  "devDependencies": {
    "lerna": "5.1.8",
    "nx": "14.4.2"
  }
}
